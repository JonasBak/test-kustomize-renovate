Repo to test using renovate with kustomize.

Run using `docker run --rm -e "RENOVATE_TOKEN=$GITHUB_TOKEN" -e LOG_LEVEL=debug -e RENOVATE_RECREATE_WHEN=always renovate/renovate jonasbak/test-kustomize-renovate`.
